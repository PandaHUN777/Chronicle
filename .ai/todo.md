# Backlog

The backlog moved to **GitHub Issues** in September 2026. This file used to
hold it (4,582 lines). It is now a pointer. **Do not add to it; open an issue.**

- Everything open is an issue, in the repo whose code has to change.
- Waiting on the operator: issues labelled `needs-operator`.
- Ideas nobody has planned yet: #740.
- An old tracking ID such as `C-HTMX-SCRIPT-SWEEP` is listed in the issue that
  replaced it, so searching the issues for the ID finds it. Code comments that
  say "booked in `.ai/todo.md`" mean that issue.
- The old file is in git history: `git show e89e9bdd:.ai/todo.md`.
