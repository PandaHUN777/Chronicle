# WebSocket Package (`internal/websocket/`)

## Purpose

Campaign-scoped WebSocket hub for real-time bidirectional communication between
Chronicle and external clients (Foundry VTT module, browser sessions). Broadcasts
domain events (entity changes, map updates) to all connections in a campaign
without polling.

## Architecture

```
HandleUpgrade (handler.go)
  → MultiAuthenticator (auth.go)       ← API key or session cookie
  → Hub.RegisterClient (hub.go)        ← creates Client, starts goroutines
       ├─ Client.readPump (client.go)  ← reads WS frames, enforces scope
       ├─ Client.writePump (client.go) ← writes queued messages, sends pings
       └─ Hub.Run() event loop         ← register/unregister/broadcast
```

Inbound: readPump reads JSON → enforces campaign scope → hub broadcast.
Outbound: service calls EventBus.Publish → hub encodes → writePump sends.
Echo suppression: hub skips the sender (msg.SenderID == client.ID).

## Files

| File | Purpose |
|------|---------|
| `message.go` | MessageType constants, Message struct, encode/decode |
| `hub.go` | Manages all clients by campaign; sequential event loop avoids races |
| `client.go` | Client struct, readPump/writePump goroutines, ping/pong keep-alive |
| `handler.go` | HTTP→WS upgrade handler, Authenticator interface, origin validation |
| `auth.go` | MultiAuthenticator: API key (query param) then session cookie fallback |
| `eventbus.go` | EventBus interface for services; hubEventBus wrapper; NoopEventBus for tests |

## Connection Parameters

writeWait 10s · pongWait 60s · pingPeriod 30s · maxMessageSize 64KB ·
sendBufferSize 256 (per client) · broadcastBuffer 256.

## Authentication (`MultiAuthenticator`)

1. **API Key** (Foundry VTT): `?token=<key>` → syncapi validates → campaignID,
   userID (key owner), role=3 (owner), source="foundry".
2. **Session Cookie** (browser): session cookie + `?campaign=<uuid>` →
   campaignID, userID, role (from membership), source="browser".
3. **Bearer Fallback**: `Authorization: Bearer <key>` header if no campaign param.

Origin is validated against the configured `BaseURL` plus any allowed origins
passed at startup, preventing cross-site WS hijacking while still allowing
Foundry VTT connections from other hosts that carry a valid API key.

## Services That Publish Events

| Service | Event Types |
|---------|-------------|
| entities | entity.created, entity.updated, entity.deleted |
| drawings | drawing.created/updated/deleted |
| tokens | token.created/moved/updated/deleted |
| maps | map.updated, fog.updated, layer.updated, marker.created/updated/deleted |

Calendar is not currently a publisher: the calendar plugin was deleted down to
a domain layer and migrations for a ground-up rebuild (#741), taking its event
publisher with it. `calendar.*` message types stay defined in `message.go` for
when the rebuild lands.

## Security Considerations

- Campaign scope enforced in readPump (msg.CampaignID = client.CampaignID)
- Sender ID set server-side (msg.SenderID = client.ID) — cannot be spoofed
- Backpressure: clients with full send buffers are disconnected
- Message type validation rejects unknown/malformed types
- All auth checked before WS upgrade (no unauthenticated connections)
- **DM-only audience gate**: a message flagged `RequiresDM` is delivered only
  to clients with Owner role or `IsDmGranted` (`permissions.CanSeeDmOnly`),
  checked server-side in `Hub.Run()` before the message is queued — the
  client-side visibility filter is not the only defense.
- **Per-user audience filter**: for a non-DM-equivalent client, `Hub.Run()`
  also checks `msg.AudienceAllows(client.UserID)`, mirroring
  `maps.VisibilityRules`' allow/deny-list predicate (an explicit deny always
  excludes; a non-empty allow-list is strict; empty means everyone).
  Deliberately duplicated rather than imported, since this package is generic
  transport infrastructure; `internal/app/map_audience_parity_test.go` pins
  that the two predicates agree.
