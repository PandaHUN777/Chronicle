# Sync API Plugin

## Purpose

Versioned REST API (v1) for external tool integration: Foundry VTT modules
and custom scripts read/write campaign data programmatically. Also provides a
bidirectional entity sync protocol, API key management, request logging,
security event tracking, and an admin monitoring dashboard.

## Tier

**Plugin** -- handlers, service, repository, middleware, admin templates.

## Files

| File | Role |
|------|------|
| `routes.go` | Route registration: admin, campaign key management, API v1 |
| `api_handler.go` | Entity CRUD, relations, bidirectional sync endpoint |
| `calendar_api_handler.go` | Calendar API surface; every route answers 503 while the calendar plugin is rebuilt (see "Calendar Endpoints") |
| `map_api_handler.go` | Map, drawing, token, layer, fog, marker CRUD |
| `note_api_handler.go` | Note CRUD (list, get, create, update, delete) |
| `tag_api_handler.go` | Tag CRUD, entity tag assignment, bulk tag operations |
| `media_api_handler.go` | Media file list/upload/delete with signed URLs |
| `sync_handler.go` | Sync mapping CRUD (Chronicle-to-external ID mappings) |
| `middleware.go` | API key auth, permission checks, campaign match, rate limiting, addon gating, `RequireJSONContentType` |
| `service.go` | Business logic: key generation (bcrypt), auth, logging, security, IP blocklist |
| `model.go` | Domain types: APIKey, APIRequestLog, SecurityEvent, IPBlock, SyncMapping |
| `repository.go` | MariaDB persistence for all sync API data |
| `egress_sanitize.go` | Defense-in-depth HTML sanitize helpers (`sanitizeEntityHTMLForEgress`, `sanitizeNoteHTMLForEgress`, `sanitizeCalendarEventHTMLForEgress`, scalar + slice variants), invoked by the `/api/v1/*` GET handlers that emit user HTML, before `c.JSON` |
| `egress_sanitize_test.go` | Per-helper polluted-row tests, nil-safety, an AST pin catching a future refactor that drops the helper call from any handler |
| `json_content_type_test.go` | Positive/negative/safe-method/sub-group-skip tests for `RequireJSONContentType` |

## How It Works

### Authentication

Prefix-lookup + bcrypt-verify: client sends `Authorization: Bearer chron_<hex>`
→ middleware extracts the 8-char prefix, looks up the key row by prefix →
full key verified against the stored bcrypt hash → key must be active and not
expired. Keys are scoped to a single campaign and carry permissions (`read`,
`write`, `sync`).

### Device Fingerprint Binding

Clients may send `X-Device-Fingerprint`. On first use it binds to the key; a
later request from a different fingerprint is rejected with a security event
logged. Prevents key sharing across devices.

### Rate Limiting

Fixed-window counter per key per minute, configurable per key (default 60,
max 1000). `X-RateLimit-Limit`/`X-RateLimit-Remaining` headers on every
response. Over limit: 429 with `Retry-After: 60`.

### Addon Gating

Calendar and maps API endpoints are gated behind `RequireAddonAPI`: 404 when
the addon is disabled for the campaign, 503 when the check fails (fail-closed).

The `sync-api` addon toggle gates the KEY, not the route: `RequireSyncAPIAddon`
sits on both `/api/v1` groups and on `AuthenticateKeyForWS`, refusing real
Bearer keys with 403 `sync_api_disabled` when the addon is off. A synthetic
session key (Chronicle's own widgets reading the API by cookie) always
short-circuits — the toggle revokes outside access, not the app's own UI.
Enforcement defaults to denied for a campaign with no `campaign_addons` row;
`CreateKey` enables the addon and a boot reconciler backfills existing
campaigns without flipping an already-disabled one back on.

### Middleware Stack

Two parallel groups at `/api/v1`:

- **`v1` (JSON)**: `RequireAuthOrAPIKey` → `RequireKeyOwnerStillOwner` →
  `RateLimit` → `RequireJSONContentType` → (per-route)
  `RequireCampaignMatch` → `RequirePermission` → optionally
  `RequireAddonAPI`. `RequireJSONContentType` rejects POST/PUT/PATCH without
  `Content-Type: application/json` with 415; safe methods pass through.
  `RequireKeyOwnerStillOwner` refuses a stored Bearer key once its creator
  is no longer an Owner of the key's campaign;
  a session-authed caller always passes through.
- **`v1Multipart`**: `RequireAuthOrAPIKey` → `RequireKeyOwnerStillOwner` →
  `RateLimit` → (per-route) `RequireCampaignMatch` → `RequirePermission`.
  Skips `RequireJSONContentType` for `multipart/form-data`. Only inhabitant
  today: `POST /api/v1/campaigns/:id/media` (`UploadMedia`); any future
  multipart endpoint mounts here, not on `v1`.

### Egress HTML sanitization

`GetEntity`, `ListEntities`, `GetNote`, `ListNotes`, `GetEvent`, `ListEvents`
invoke an `egress_sanitize.go` helper before `c.JSON(...)` to re-sanitize HTML
fields on the response copy (`sanitize.HTMLPtr` is the nullable companion to
`sanitize.HTML`); helpers operate on fresh DB-scanned models, so the canonical
row is untouched. This is defense-in-depth: ingress sanitization on the write
path (each source plugin's `service.go`) is the primary defense; egress
covers historical rows or tooling-bug-inserted rows that slipped past
ingress. Scope is `/api/v1/*` Foundry-bound surfaces only — the
backup/restore round-trip (`internal/app/export_adapters.go`,
`campaigns/export_handler.go`, `internal/plugins/restore/`) is a deliberate
lossless carve-out and must not be re-sanitized.

## API Endpoints (v1)

All under `/api/v1/campaigns/:id` with API key auth.

### Entity, Type, Tag & Relation Endpoints

| Method | Path | Permission | Description |
|--------|------|------------|-------------|
| GET | `/` | read | Campaign details |
| GET | `/members` | read | List campaign members (user_id, display_name, role, avatar) |
| GET | `/entity-types` | read | List entity types |
| GET | `/entity-types/:typeID` | read | Get entity type |
| POST | `/entity-types` | write | Create entity type |
| PUT | `/entity-types/:typeID` | write | Update entity type |
| GET | `/entities` | read | List entities (paginated, ?q=&type_id=&page=&per_page=) |
| GET | `/entities/:entityID` | read | Get single entity (privacy enforced) |
| GET | `/entities/:entityID/relations` | read | List entity relations |
| POST | `/entities` | write | Create entity |
| PUT | `/entities/:entityID` | write | Update entity |
| PUT | `/entities/:entityID/fields` | write | Update custom fields only |
| DELETE | `/entities/:entityID` | write | Delete entity |
| POST | `/entities/bulk-update` | write | Bulk entity type reassignment (max 200) |
| GET | `/addons` | read | List all addons with enabled/installed state |
| GET | `/tags` | read | List campaign tags (DM-only filtered by role) |
| POST | `/tags` | write | Create tag |
| PUT | `/tags/:tagId` | write | Update tag |
| DELETE | `/tags/:tagId` | write | Delete tag |
| PUT | `/entities/:entityID/tags` | write | Set entity tags (replace all) |
| POST | `/entities/bulk-tags` | write | Bulk tag add/remove/set (max 200 entities) |
| GET | `/relations/types` | read | List predefined relation type pairs |
| POST | `/entities/:entityID/relations` | write | Create relation (bidirectional) |
| PUT | `/relations/:relationId` | write | Update relation metadata |
| DELETE | `/relations/:relationId` | write | Delete relation and its reverse |

### Sync Endpoints

| Method | Path | Permission | Description |
|--------|------|------------|-------------|
| POST | `/sync` | sync | Bidirectional sync (pull since + push changes) |
| GET | `/sync/mappings` | sync | List ID mappings (paginated) |
| GET | `/sync/mappings/:mappingID` | sync | Get single mapping |
| POST | `/sync/mappings` | sync | Create ID mapping |
| DELETE | `/sync/mappings/:mappingID` | sync | Delete ID mapping |
| GET | `/sync/lookup` | sync | Lookup by Chronicle or external identity |
| GET | `/sync/pull` | sync | Pull mappings modified since timestamp |

### Calendar Endpoints (requires calendar addon)

The calendar plugin was deleted down to a domain layer and migrations for a
ground-up rebuild (V5, #741); the syncapi calendar routes stay registered but
every one — `GetCalendar`, `GetCurrentDate`, `ListEvents`, `GetEvent`,
`ExportCalendar`, `CreateEvent`, `UpdateEvent`, `DeleteEvent`,
`UpdateCalendarSettings`, `UpdateMonths`, `UpdateWeekdays`, `UpdateMoons`,
`UpdateEras`, `AdvanceDate`, `SetDate`, `AdvanceTime`, `ImportCalendar`,
`GetWorldState`, `ConfirmDate`, and the rest — answers:

```
HTTP 503
{"error":"calendar_rebuilding","message":"Chronicle's calendar is being rebuilt and is temporarily unavailable. Calendar sync is paused; maps, actors, items and notes are unaffected."}
```

503 rather than 404 on purpose: a 404 sends the Foundry module down its
too-old-to-have-the-endpoint compatibility path and hides the real reason.
`calendar_api_handler.go` documents the CALV5-PLACEHOLDER swap-back: every
method name matches the real handler it replaces, so nothing else changes
when the rebuild lands.

### Calendar Date Beacon (member-read, no addon gate)

| Method | Path | Auth | Description |
|--------|------|------|-------------|
| GET | `/campaigns/:id/calendar-sync-beacon` | session, any member | Read the served + applied date beacon |

This endpoint and its storage (`sync_calendar_date_beacons`) are syncapi's own
and outlive the calendar plugin; not stubbed by the blackout. In practice they
stay dormant while `GetCurrentDate` and `ConfirmDate` — the only writers —
answer 503 instead of recording a beacon, so the endpoint serves whatever was
last written before the rebuild, or nothing. Not under
`/api/v1/campaigns/:id/calendar/*` — it's a plain web-session endpoint
(`RegisterCampaignRoutes`'s `cg` group).

### Media Endpoints

| Method | Path | Permission | Description |
|--------|------|------------|-------------|
| GET | `/media` | read | List media (paginated) |
| GET | `/media/stats` | read | Storage statistics |
| GET | `/media/:mediaID` | read | Get file metadata with signed URL |
| POST | `/media` | write | Upload file (multipart, 10MB limit) |
| DELETE | `/media/:mediaID` | write | Delete file |

### Map Endpoints (requires maps addon)

Full CRUD for maps, drawings, tokens, layers, fog of war, and markers. Read
endpoints need `read`; writes need `write`. Includes optimized
`PATCH .../tokens/:tokenID/position` for real-time drag sync.

| Method | Path | Permission | Description |
|--------|------|------------|-------------|
| GET | `/maps` | read | List maps |
| GET | `/maps/:mapID` | read | Get map with embedded markers |
| GET | `/maps/:mapID/markers` | read | List markers (role-filtered) |
| GET | `/maps/:mapID/markers/:markerID` | read | Get single marker |
| POST | `/maps/:mapID/markers` | write | Create marker |
| PUT | `/maps/:mapID/markers/:markerID` | write | Update marker |
| DELETE | `/maps/:mapID/markers/:markerID` | write | Delete marker |
| GET | `/maps/:mapID/drawings` | read | List drawings |
| POST | `/maps/:mapID/drawings` | write | Create drawing |
| PUT | `/maps/:mapID/drawings/:drawingID` | write | Update drawing |
| DELETE | `/maps/:mapID/drawings/:drawingID` | write | Delete drawing |
| GET | `/maps/:mapID/tokens` | read | List tokens |
| POST | `/maps/:mapID/tokens` | write | Create token |
| PUT | `/maps/:mapID/tokens/:tokenID` | write | Update token |
| PATCH | `/maps/:mapID/tokens/:tokenID/position` | write | Move token (optimized) |
| DELETE | `/maps/:mapID/tokens/:tokenID` | write | Delete token |
| GET | `/maps/:mapID/layers` | read | List layers |
| POST | `/maps/:mapID/layers` | write | Create layer |
| PUT | `/maps/:mapID/layers/:layerID` | write | Update layer |
| DELETE | `/maps/:mapID/layers/:layerID` | write | Delete layer |
| GET | `/maps/:mapID/fog` | read | List fog regions |
| POST | `/maps/:mapID/fog` | write | Create fog region |
| DELETE | `/maps/:mapID/fog/:fogID` | write | Delete fog region |
| DELETE | `/maps/:mapID/fog` | write | Reset all fog |

### Note Endpoints

| Method | Path | Permission | Description |
|--------|------|------------|-------------|
| GET | `/notes` | read | List notes visible to API key owner |
| GET | `/notes/:noteID` | read | Get single note |
| POST | `/notes` | write | Create note |
| PUT | `/notes/:noteID` | write | Update note |
| DELETE | `/notes/:noteID` | write | Delete note |

## Sync Protocol

`POST /sync` supports bidirectional sync in a single request:

- **Pull**: set `since` (RFC3339) to receive entities modified after that
  time. One request walks at most 10 internal pages of 100. If the campaign
  has more, the response sets `has_more` and `next_cursor`; the client
  re-POSTs the same `since` with that cursor until `has_more` is false, then
  uses the `server_time` from the *first* response of the walk as the next
  `since` (not the last, which would skip anything modified mid-walk).
- **Push**: set `changes` array with `{action, entity_id, ...}` objects.
  Actions: `create`, `update`, `delete`. Max 2000 changes per request. Each
  change's content fields are `patch.Field`: on `update` an absent field
  preserves the stored value, present replaces it; `create` has nothing to
  preserve so reads each field's zero default.
- **Response**: `server_time` for use as the next `since`, `entities` array
  of pulled data, `next_cursor` when `has_more` is true, and a `results`
  array with per-change status.

## Dashboard Integration

- `sync_status` dashboard block type for campaign dashboard editor.
- `GET /campaigns/:id/sync-status` — HTMX fragment: active API key count, 24h
  request stats, per-key status with last-used timestamps (Owner only).
- Lazy-loaded via `hx-trigger="intersect once"` in the dashboard block.

## Admin Routes

Under `/admin/api` (site admin only): dashboard, request logs, security
events (with resolve), IP blocklist management (block/unblock), key
toggle/revoke.

## Dependencies

- **Uses:** entities, campaigns, calendar, maps, media, relations, addons, notes, tags plugins
- **Used by:** Foundry VTT sync module, external scripts, admin dashboard

## Notable Design Decisions

- Keys use bcrypt hashing with prefix-based lookup to avoid full-table scans.
- Rate limiter is in-memory (not Redis), acceptable for single-instance deployments.
- Request logging and security events are fire-and-forget goroutines so they
  don't block API responses.
- IDOR protection on every endpoint: entity/event/media ownership verified
  against the key's campaign before any operation. `PUT`/`DELETE` on a
  relation or note resolve the object and 404 on a campaign or ownership
  mismatch before running the owning plugin's visibility/ownership gate —
  never trust the campaign match alone for an object addressed by its own id.
- WebSocket auth reuses the same key validation path via
  `AuthenticateKeyForWS`, returning owner-level role for API key holders —
  but only after confirming the key's creator is still an Owner of the
  key's campaign (the WS twin of the REST gate
  `RequireKeyOwnerStillOwner`); a demoted or
  removed creator's key is refused instead of silently granted Owner role.
- Media API responses include HMAC-signed URLs when a signer is configured,
  1-hour expiry.
- `GetCurrentDate`'s dm_only visibility gating on celestial/world-state reads
  uses `resolveRole` and the caller's own forwarded identity — a
  `sync`-permission key resolves to Owner, a read/write key to Player.
  Deliberately no second filter: a divergent second visibility path on the
  same data is how a prior entity-ties leak happened.
- `apiUpdateEntityRequest` and `syncChange` use `patch.Field` for every
  content field — `parent_id`, `is_private`, `type_label`, `entry`, `name` —
  so a narrow client body (e.g. a Foundry actor-sync push sending only
  `{name}`) cannot silently detach an entity from its parent, un-private a
  hidden character, or blank its type label.
