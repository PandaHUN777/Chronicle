# Notes Widget

## Purpose
Floating personal note-taking panel (Google Keep-style) that appears in the
bottom-right corner of campaign pages. Each user has their own private notes
within a campaign, optionally scoped to specific entities (pages). Notes can
be shared with all campaign members via the `is_shared` flag.

## Architecture
- **Tier:** Widget (with full backend)
- **Addon slug:** `notes` (disabled by default, owner enables per-campaign)
- **Backend:** handler → service → repository → MariaDB `notes` + `note_versions` tables
- **Sync API:** Full CRUD at `/api/v1/campaigns/:id/notes` (via `internal/plugins/syncapi/note_api_handler.go`)
- **Frontend:** `static/js/widgets/notes.js` — self-contained floating panel

## Data Model

### notes table (migration 000017 + 000022)
- Per-user, per-campaign, optional `entity_id` scoping
- Content is a JSON array of blocks: `[{type: "text", value: "..."}, {type: "checklist", items: [...]}]`
- Rich text: `entry` (ProseMirror JSON) + `entry_html` (pre-rendered HTML) dual storage
- Collaboration: `is_shared`, `last_edited_by`, `locked_by`, `locked_at`
- Pinned notes sort first, then by `updated_at` DESC

### note_versions table (migration 000022)
- Snapshot of note content at a point in time (title, content, entry, entry_html)
- FK cascade delete from `notes.id`
- Max 50 versions per note (`MaxVersionsPerNote`), oldest auto-pruned

### note_attachments table (migration 000005)
- Per-note audio file attachments (voice memos, session recordings)
- `id` (CHAR(26) ULID), `note_id` (FK CASCADE), `campaign_id`
- `file_path`, `original_name`, `mime_type`, `file_size`
- `duration_secs` (INT nullable), `transcript` (LONGTEXT nullable — manual or AI-generated)
- `created_at`, `updated_at`

## Key Features

### Core
- **Page-specific notes:** When viewing an entity, shows notes for that page
- **Campaign-wide notes:** General notes not tied to any entity
- **Checklists:** Interactive checkboxes with toggle API
- **Collapsible panel:** Minimizes to a small FAB, expands on click
- **Color coding:** Notes can have a color accent
- **Text size:** S/M/L settings gear (localStorage persistence)
- **Resizable panel:** Drag top-left corner to resize (localStorage persistence)

### Shared Notes
- **Share toggle:** Owner can make a note visible to all campaign members
- **Shared badge:** Non-owners see a users icon on shared notes
- **Access control:** Owner can edit all fields; non-owners can edit content only
  (cannot change pinned/shared status)
- **Delete:** Only the note owner can delete

### Pessimistic Edit Locking
- **Lock acquisition:** When editing a shared note, the widget POSTs to `/lock`
  before entering edit mode. If another user holds the lock, edit is denied.
- **Heartbeat:** 2-minute interval keeps the lock alive (server timeout is 5 min)
- **Lock release:** On "Done" click or panel close, the lock is released
- **Stale lock reclamation:** Locks older than 5 minutes are automatically
  reclaimed by the database query
- **Force unlock:** Campaign owners can force-unlock via `/force-unlock` endpoint
- **Lock indicator:** Locked-by-another notes show a lock icon, edit button hidden

### Version History
- **Snapshot-on-save:** Every `Update` and `RestoreVersion` saves the current
  state as a version before applying changes
- **Version list:** Sub-panel shows version history with title and timestamp
- **One-click restore:** Restoring a version creates a pre-restore snapshot,
  then applies the old version's content
- **Auto-pruning:** Oldest versions beyond `MaxVersionsPerNote` (50) are deleted

### Rich Text
- **Dual storage:** `entry` stores ProseMirror JSON, `entry_html` stores
  pre-rendered HTML (sanitized server-side)
- **Display:** When `entryHtml` is present on a note (view mode), it's rendered
  as HTML; legacy block content is used as fallback
- **Editing:** Rich text editing uses legacy blocks for now; full TipTap
  integration in the notes panel itself is not yet built

## Routes
All under `/campaigns/:id/notes`:

| Method | Path | Handler | Description |
|--------|------|---------|-------------|
| GET | `/` | List | List notes (scope=all\|campaign\|entity) |
| POST | `/` | Create | Create note |
| PUT | `/:noteId` | Update | Update note (creates version snapshot) |
| DELETE | `/:noteId` | Delete | Delete note (owner only) |
| POST | `/:noteId/toggle` | ToggleCheck | Toggle checklist item |
| POST | `/:noteId/lock` | Lock | Acquire edit lock |
| POST | `/:noteId/unlock` | Unlock | Release edit lock |
| POST | `/:noteId/heartbeat` | Heartbeat | Keep lock alive |
| POST | `/:noteId/force-unlock` | ForceUnlock | Force release (owner only) |
| GET | `/:noteId/versions` | ListVersions | Version history |
| GET | `/:noteId/versions/:vid` | GetVersion | Get specific version |
| POST | `/:noteId/versions/:vid/restore` | RestoreVersion | Restore to version |
| GET | `/:nid/attachments` | ListAttachments | List audio attachments for note |
| POST | `/:nid/attachments` | UploadAttachment | Upload audio file (multipart) |
| DELETE | `/:nid/attachments/:aid` | DeleteAttachment | Remove attachment |
| PUT | `/:nid/attachments/:aid/transcript` | UpdateTranscript | Save transcript text |

## Frontend Widget
Registered as `Chronicle.register('notes', ...)`. Mounted via
`<div data-widget="notes" data-campaign-id="..." data-entity-id="..." data-user-id="...">` in the
app layout. The widget renders a fixed-position panel in the bottom-right.

### State
- `open`, `tab`, `notes`, `pageNotes`, `editingId`, `loading` (core)
- `lockedNoteId`, `lockHeartbeatTimer` (locking)
- `versionsNoteId`, `versions`, `versionsLoading` (version history)

### CSS Classes (in input.css)
- `.note-shared` — left accent border on shared notes
- `.note-shared-badge` — users icon badge
- `.note-lock-badge` — lock icon badge
- `.notes-lock-toast` — error toast animation
- `.note-entry-html` — rich text display container
- `.notes-versions-*` — version history sub-panel

## Audio Attachments

Audio file attachments for journal notes.

### Backend
- **`MediaUploader` interface** in `handler.go` — adapter wrapping the media plugin's `UploadRaw` method
  for multipart file uploads. Wired in `app/routes.go` via `mediaUploadAdapter`.
- **`AttachmentRepository`** interface in `repository.go` — CRUD for `note_attachments` table.
  Methods: `CreateAttachment`, `ListByNote`, `FindAttachmentByID`, `DeleteAttachment`, `UpdateTranscript`.
- **`AttachmentService`** interface in `service.go` — business logic layer.
  Methods: `ListAttachments`, `GetAttachment`, `CreateAttachment`, `DeleteAttachment`, `UpdateTranscript`.
- Upload handler reads multipart file, pipes through `MediaUploader` (inherits media plugin's
  MIME validation, magic bytes check, quota enforcement), then creates `NoteAttachment` record.
- Allowed audio MIME types: `audio/mpeg`, `audio/ogg`, `audio/wav`, `audio/webm`.

### Journal UI (`journal.js`)
- **Upload button**: Microphone icon (`<label>` wrapping hidden file input) in note toolbar.
  Accepts audio MIME types only.
- **Attachments area**: `#journal-attachments` div between editor and status bar in `journal.templ`.
  Hidden when no attachments.
- **Audio player**: Inline `<audio>` element with native controls per attachment.
- **Transcript**: Collapsible section per attachment with editable textarea and "Save transcript" button.
  Submits via `PUT /:nid/attachments/:aid/transcript`.
- **Delete**: Trash icon per attachment with confirmation dialog.
- Attachments auto-load when a note is selected (`loadAttachments` in `selectNote`).

### Journal @Mentions
The journal's TipTap editor includes `MentionLink` mark (preserves `data-mention-id`
and `data-entity-preview` attributes) and `MentionExtension` lifecycle hooks (onCreate,
onUpdate, onKeyDown, onDestroy). Users can type `@` to search entities and insert mention
links with tooltip cards, matching the main entity editor's behavior.

## Real-Time Sync

`NoteEventPublisher` interface emits domain events when notes change:
- `note.created` — after Create
- `note.updated` — after Update
- `note.deleted` — after Delete

Implemented by `noteEventPublisherAdapter` in `app/routes.go` which publishes
to the WebSocket EventBus. External clients (Foundry VTT) receive these events
on their WebSocket connection for real-time sync.

## Dependencies
- **Uses:** `internal/apperror`, `internal/plugins/auth` (GetUserID, RequireAuth),
  `internal/plugins/campaigns` (RequireCampaignAccess, RequireRole, GetCampaignContext),
  `internal/plugins/media` (via `MediaUploader` adapter for audio uploads)
- **Used by:** App layout (`NotesWidget()` templ component), journal page,
  syncapi plugin (note CRUD API endpoints, WS events)

## File Map
| File | Purpose |
|------|---------|
| model.go | Note, NoteVersion, NoteAttachment, Block, ChecklistItem structs; request DTOs; `CanAccess`/`IsOwnedBy`/`IsLocked`/`IsLockedByUser` methods |
| repository.go | NoteRepository + AttachmentRepository interfaces + MariaDB impl; `ListSharedByCampaign` (no per-user filter — see Access control below) |
| service.go | NoteService + AttachmentService + NoteEventPublisher interfaces + impl (business logic, version snapshots, lock management, attachment CRUD, event publishing) |
| handler.go | HTTP handlers (thin: bind, call service, render); `canAccessNote` helper; MediaUploader interface; attachment handlers |
| routes.go | Route registration on Echo campaign group (notes + attachments) |
| journal.templ | Full-page journal template with audio upload button and attachments area |
| service_test.go | Unit tests with mock repository |
| members_api_shape_test.go | Pins the `GET /campaigns/:id/notes/members` response shape (see below) |
| .ai.md | This file |

## Access control

**`Note.CanAccess`/`Note.IsOwnedBy` on the model are the one ownership
implementation — use them, do not re-derive.** The service and repository
address a note by bare `WHERE id = ?` with no user predicate at any layer, so
the handler is the only gate: `canAccessNote` in this widget's handler
delegates to `CanAccess` (owner, campaign-wide share, or explicit per-user
share), and `internal/plugins/syncapi/note_api_handler.go` gates `GET`/`PUT`
on `CanAccess` and `DELETE` on `IsOwnedBy` (reading a shared note is not
authority to destroy it). The syncapi `UpdateNote` also strips
`is_shared`/`shared_with`/`pinned` from a non-owner's patch. Pinned by
`internal/plugins/syncapi/note_ownership_test.go`.

**`GET /campaigns/:id/notes/members` returns `memberRef` =
`{user_id, username, role}`.** The share-with-players picker
(`notes.js`) must read those exact keys — `members_api_shape_test.go` asserts
the body is a bare array with rows carrying `user_id`/`username`/`role` and
neither `id` nor `name`, and `test/js/notes_share_picker.test.mjs` parses the
json tags off `type memberRef struct` rather than hardcoding them. Renaming
either side reddens both files.

**`ListSharedByCampaign` (repository + service) is the only list method in
this package with no per-user filter** — deliberate: the shared-note corpus
is campaign data rather than a person's. It is owner-gated by its single
caller (`noteExportAdapter`) and reachable from no HTTP route. Exposing it to
a handler re-opens a cross-user read.

## Export / import

What leaves the instance: `is_shared=TRUE` notes only. `shared_with` target
user ids are dropped (they name users who don't exist in the destination).
Personal notes never leave. `checklist` content lives only in the legacy
`content` block field, so `ExportNote` carries it even though `entry`/
`entry_html` are the primary storage. Folder membership travels as a
`parent_index` into the notes slice, because note ids are not stable across
instances; import runs two passes (create flat, then re-parent) since a
folder may appear after its children. `entry`/`entry_html` are applied
through `Update` on import so imported HTML meets the sanitizer.

Pins: `internal/app/export_notes_roundtrip_test.go` and
`internal/wire/export_adapter_wiring_test.go` (an AST pin requiring
`routes.go` to call all twenty export/import setters).

## Testing
- Service tests cover Create, Update, Delete, ToggleCheck, List queries, ID
  generation, and edge cases, with a mock repository implementing every
  interface method as a function field.
